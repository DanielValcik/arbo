# B2 Challenger: tighter edge filter
#
# Hypothesis: champion MIN_EDGE=0.08 is permissive. Tighter floor
# (0.12) sacrifices volume for precision. Tested vs B3/B3_15M parallel
# hypothesis to see if tighter-edge improves WR universally.
#
# Status: shadow only — no capital risk.

variant_id: ch_edge_tight
strategy: B2
status: challenger
parent_variant: champion_v1
notes: |
  MIN_EDGE 0.08 → 0.12. All other params identical to champion_v1.
  Expected: fewer trades, higher WR.

params:
  MIN_EDGE: 0.12              # ← CHANGED (was 0.08)
  MAX_EDGE: 0.90
  MIN_PRICE: 0.05
  MAX_PRICE: 0.60
  MIN_TIME_TO_EXPIRY_H: 8
  MAX_TIME_TO_EXPIRY_H: 168
  MIN_HOLD_EDGE: 0.03
