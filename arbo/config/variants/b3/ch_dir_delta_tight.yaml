# B3 5-min Challenger: direction-delta filter tighter
#
# Hypothesis: champion V6.0 dir_delta cap = $15 (Binance-Chainlink gap in
# our direction). Tighter cap forces stricter oracle alignment — trades
# only fire when both oracles confirm the move. Cost: fewer trades.
# Benefit: fewer phantom trades where only Binance moved.
#
# Status: challenger (Phase 2B — shadow only, no capital)

variant_id: ch_dir_delta_tight
strategy: B3
status: challenger
parent_variant: champion_v1
notes: |
  LIVE_MAX_DIR_DELTA 15 → 10. All other params identical to champion_v1.
  Expected: fewer trades, stricter oracle consensus, potentially higher WR.

params:
  SIGMA_WINDOW: 1440
  SIGMA_METHOD: realized
  SIGMA_SCALE: 0.348
  SIGMA_FLOOR: 0.00005
  ENTRY_THRESHOLD: 0.020
  MIN_ENTRY_MIN: 1
  MAX_ENTRY_MIN: 3
  CONTRARIAN: false
  PAPER_MATCH_LIVE: true
  PROFIT_TARGET: 0.207
  STOP_LOSS: 99.0
  MAX_HOLD_MIN: 3
  EDGE_EXIT: 0.076
  EXIT_BEFORE_END: 0
  ALLOW_RESOLUTION: true
  USE_BTC_STOP: true
  BTC_STOP_PCT: 0.0015
  SPREAD: 0.060
  MAKER: true
  MIN_ENTRY_MKT_FV: 0.413
  MAX_ENTRY_MKT_FV: 0.570
  LIVE_MIN_EDGE: 0.30
  LIVE_MIN_BTC_MOVE: 35.0
  LIVE_MAX_VELOCITY: 60.0
  LIVE_MAX_DIR_DELTA: 10.0         # ← CHANGED (was 15.0)
  LIVE_MAX_FILL_PRICE: 0.75
  POSITION_PCT: 0.026
  EDGE_SCALING: 10.0
  MAX_BET_SIZE: 100.0
  MAX_SHARES: 500
  MIN_ORDER_SIZE: 5.0
  WINDOW_MIN: 5
  REENTRY_COOLDOWN: 2
