# B3 5-min Champion v1 — V6.0 deployed 2026-04-06 18:27 UTC (commit 596d5fd)
#
# Source: 278 live-trade analysis (pre-V6.0) → Dual Filter design
# V6.0 live performance: 28 trades, WR 67.9%, PnL -$2.39 (below breakeven by 4.4pp,
#   within 1σ noise at N=28; hold per Framework §10.1)
#
# All constants mirror arbo/strategies/b3_quality_gate.py at V6.0 deploy.
# DO NOT edit this file when tuning; create a new challenger YAML instead.

variant_id: champion_v1
strategy: B3
status: champion
parent_variant: null
notes: |
  V6.0 Dual Filter (velocity + dir_delta) deployed 2026-04-06 18:27 UTC.
  Designed from 278-trade live analysis pre-V6.0.
  Fill cap 0.75 added 2026-04-10 (commit 3c3e19c).

params:
  # Volatility
  SIGMA_WINDOW: 1440
  SIGMA_METHOD: realized
  SIGMA_SCALE: 0.348
  SIGMA_FLOOR: 0.00005

  # Entry
  ENTRY_THRESHOLD: 0.020
  MIN_ENTRY_MIN: 1
  MAX_ENTRY_MIN: 3
  CONTRARIAN: false

  # Exit (never-sell, paper matches live)
  PAPER_MATCH_LIVE: true
  PROFIT_TARGET: 0.207
  STOP_LOSS: 99.0
  MAX_HOLD_MIN: 3
  EDGE_EXIT: 0.076
  EXIT_BEFORE_END: 0
  ALLOW_RESOLUTION: true

  # BTC-price stop (paper-only if PAPER_MATCH_LIVE=True)
  USE_BTC_STOP: true
  BTC_STOP_PCT: 0.0015

  # Market model
  SPREAD: 0.060
  MAKER: true

  # Price bounds (paper scanner)
  MIN_ENTRY_MKT_FV: 0.413
  MAX_ENTRY_MKT_FV: 0.570

  # Live filters (V6.0 + data-driven 2026-04-12 relaxation)
  LIVE_MIN_EDGE: 0.30
  LIVE_MIN_BTC_MOVE: 35.0
  LIVE_MAX_VELOCITY: 60.0
  LIVE_MAX_DIR_DELTA: 15.0
  LIVE_MAX_FILL_PRICE: 0.75     # Data-driven cap from 278-trade analysis

  # Sizing
  POSITION_PCT: 0.026
  EDGE_SCALING: 10.0
  MAX_BET_SIZE: 100.0
  MAX_SHARES: 500
  MIN_ORDER_SIZE: 5.0

  # Window
  WINDOW_MIN: 5

  # Re-entry
  REENTRY_COOLDOWN: 2
