# B3_15M Champion v1 — deployed 2026-04-12
#
# Source: shadow autoresearch rank #1, 5-fold CV on 144 resolved signals
# Shadow performance: 49 qualified trades, WR 94.4%, +$9.89, avg $0.222/share, σ=0.103
# Live performance at time of championing: N=12, WR 69.2%, PnL -$3.69 (below breakeven
#   by 2.3pp, but inside 1σ of shadow given N=12 is small — Bayesian posterior
#   still positive; hold per Framework §10.1)
#
# All constants mirror arbo/strategies/b3_15m_quality_gate.py at v1.0 deploy.
# DO NOT edit this file when tuning; create a new challenger YAML instead.

variant_id: champion_v1
strategy: B3_15M
status: champion
parent_variant: null
notes: |
  Shadow autoresearch rank #1 (2026-04-12 144-signal sweep, 5-fold CV).
  All 5 folds positive, WR 94.4%, avg $0.222/share, σ=0.103.
  Deployed live 2026-04-12 08:33 UTC.

params:
  # Volatility
  SIGMA_WINDOW: 1440
  SIGMA_METHOD: realized
  SIGMA_SCALE: 0.526
  SIGMA_FLOOR: 0.00005

  # Entry
  ENTRY_THRESHOLD: 0.089
  MIN_ENTRY_MIN: 4
  MAX_ENTRY_MIN: 11
  CONTRARIAN: false

  # Exit (never-sell, paper matches live)
  PAPER_MATCH_LIVE: true
  PROFIT_TARGET: 0.207
  STOP_LOSS: 99.0
  MAX_HOLD_MIN: 12
  EDGE_EXIT: 0.076
  EXIT_BEFORE_END: 0
  ALLOW_RESOLUTION: true

  # BTC-price stop (paper-only if PAPER_MATCH_LIVE=True)
  USE_BTC_STOP: true
  BTC_STOP_PCT: 0.0025

  # Market model
  SPREAD: 0.010
  MAKER: true

  # Price bounds (paper scanner)
  MIN_ENTRY_MKT_FV: 0.30
  MAX_ENTRY_MKT_FV: 0.95

  # Live filters (from shadow rank #1 rules)
  LIVE_MIN_EDGE: 0.30
  LIVE_MIN_BTC_MOVE: 0.0
  LIVE_MAX_BTC_MOVE: 80.0
  LIVE_MAX_MARKET_GAP: 0.30
  LIVE_MAX_FILL_PRICE: 1.01     # uncapped (15-min specific; 5-min has 0.75)
  LIVE_MAX_VELOCITY: 100.0
  LIVE_MAX_DIR_DELTA: 25.0

  # Sizing
  POSITION_PCT: 0.044
  EDGE_SCALING: 6.757
  MAX_BET_SIZE: 20.0
  MAX_SHARES: 500
  MIN_ORDER_SIZE: 5.0

  # Window
  WINDOW_MIN: 15

  # Re-entry
  REENTRY_COOLDOWN: 3
